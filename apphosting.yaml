# Settings to manage and configure a Firebase App Hosting
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # The command to start your web server.
  runCommand: npm run start

env:
  - variable: NODE_ENV
    value: production